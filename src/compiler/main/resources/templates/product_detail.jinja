<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"></meta>
    <title>{{ product.name }} - Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 800px;
            margin: 50px auto;
        }
        img {
            max-width: 400px;
        }
        .price {
            font-size: 24px;
            color: darkgreen;
            font-weight: bold;
        }
        .delete {
            color: red;
            font-weight: bold;
            font-size: 18px;
        }
    </style>
</head>
<body>
    <h1>{{ product.name }}</h1>

    <img src="{{ product.photo }}" alt="{{ product.name }}"></img>

    <p class="price">Price: {{ product.price }} $</p>

    <p><strong>Details:</strong></p>
    <p>{{ product.details }}</p>

    {% if product.stock > 0 %}
        <p>In stock: {{ product.stock }} items</p>
    {% else %}
        <p style="color: red;">Out of stock</p>
    {% endif %}

    <p><a href="/">Back to List</a></p>

    <p class="delete">
        <a href="/delete/{{ product.id }}" onclick="return confirm('Are you sure?')">Delete this product</a>
    </p>
</body>
</html>